const program = require("comandi");
program.command("install").action(async () => {
  console.log("Installing LiveOS...");
});
// Add your installation logic here, prompting user for input etc. console.log('Live-os installation complete'); }) .parse();
