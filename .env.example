# LiveOS Configuration Example
# Copy this file to .env and customize as needed

# ============================================
# Server Configuration
# ============================================

# HTTP Port (default: 3000)
PORT=3000
LIVEOS_HTTP_PORT=3000

# Node Environment (development, production)
NODE_ENV=production

# ============================================
# Domain Configuration
# ============================================

# Custom domain for accessing LiveOS (optional)
# Examples: home.local, server.local, liveos.home, myserver.lan
# LIVEOS_DOMAIN=home.local

# After setting a domain, add it to your DNS or hosts file:
#   Linux/Mac:  /etc/hosts
#   Windows:    C:\Windows\System32\drivers\etc\hosts
# Example entry:
#   192.168.1.100  home.local

# ============================================
# Docker Configuration (for future use)
# ============================================

# Docker socket path
# DOCKER_SOCKET=/var/run/docker.sock

# Docker host (if using remote Docker)
# DOCKER_HOST=unix:///var/run/docker.sock

# ============================================
# App Data Directory
# ============================================

# Directory for storing app data (for installed apps)
# APP_DATA_DIR=/opt/live-os/app-data

# ============================================
# Security (for future use)
# ============================================

# Session secret for authentication
# SESSION_SECRET=change-this-to-a-random-string

# Admin username
# ADMIN_USERNAME=admin

# ============================================
# Database (for future use)
# ============================================

# Database URL (SQLite, PostgreSQL, etc.)
# DATABASE_URL=sqlite:///opt/live-os/data/liveos.db

# ============================================
# Monitoring (for future use)
# ============================================

# Enable metrics collection
# ENABLE_METRICS=true

# Metrics update interval (in seconds)
# METRICS_INTERVAL=3

# ============================================
# Logging
# ============================================

# Log level (error, warn, info, debug)
# LOG_LEVEL=info

# Log file path
# LOG_FILE=/var/log/liveos/liveos.log
